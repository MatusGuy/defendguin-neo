#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <cinttypes>

enum class Dir : uint8_t {
	LEFT, RIGHT
};

enum InputActions : uint8_t {
	UP,
	DOWN,
	LEFT,
	RIGHT,
	FIRE
};

#endif // CONSTANTS_HPP
